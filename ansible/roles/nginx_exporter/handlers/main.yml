---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart nginx_exporter
  ansible.builtin.systemd:
    name: nginx_exporter
    state: restarted
    enabled: true
